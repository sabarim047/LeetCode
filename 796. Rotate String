class Solution {
    public boolean rotateString(String s, String goal) {
     StringBuilder sb=new StringBuilder(s+s);
     if(sb.toString().contains(goal)&& s.length()==goal.length()){
        return true;
     }
     return false;
    }
}
